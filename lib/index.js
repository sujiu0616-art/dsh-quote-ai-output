// dsh-quote-ai-output — host half (empty shell).
//
// This plugin is purely a browser feature: quoting AI output lives entirely
// in the client half (lib/client.js). The host half exists only so the
// cordis row resolves; it contributes nothing.

export const name = 'dsh-quote-ai-output'

export function apply() {
  // 纯浏览器功能；host 无需任何实现。
}
